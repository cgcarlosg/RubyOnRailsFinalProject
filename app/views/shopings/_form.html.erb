<%= form_with(model: shoping) do |form| %>
  <% if shoping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shoping.errors.count, "error") %> prohibited this shoping from being saved:</h2>

      <ul>
        <% shoping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.number_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
